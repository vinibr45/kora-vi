# Config

This directory is reserved for future implementation configuration.

Configuration should not be confused with architecture, knowledge, project context, or memory.

No runtime configuration is required in v0.1.

