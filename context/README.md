# Context

This directory is reserved for context selection and context assembly policies.

The Context Layer may read Knowledge, Project Context, and Memory, then produce a task-specific working context.

It should not become the source of truth for the information it selects.

